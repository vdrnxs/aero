# pnpm configuration
# Use hoisted linker for Docker compatibility while maintaining local benefits
node-linker=hoisted

# Note: With hoisted mode:
# - Local: Uses hardlinks to global store (saves space)
# - Docker: Works without symlink issues
# - Trade-off: Allows phantom dependencies (less strict than default pnpm)
